<!DOCTYPE html>
<html>
  <head>
    <title>Mother's Groups Search</title>
      <%= favicon %>
      <%= stylesheet 'application' %>
      <%= stylesheet 'bootstrap' %>

  </head>
  <body>
    <%= flash[:notice] %>
    <%= flash[:alert] %>
    
    <% if user_signed_in? %>
      You are logged in as <%= current_user.name %>
    <% end %>
    <%= yield %>
  </body>
</html>
